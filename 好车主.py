# 好车主的绑车以及换绑流程
# 流程一

from flask import app.current.logger,g
import numpy as np 

class intention_recongnition(questions)

	def get_question(kwargs)
		sentence = kwargs.get_question("sentence",)
		if "查违章" in questions :
			return {get_into_searching: True, {"进入查违章流程":}re}
		else
			return {get_into_FAQ: True, {"对不起小保正在开小差，请稍后再试":}}


	def get_into_searching(get_question):
		import re
        return {"您已进入查违章流程，请继续下一步操作":}
        query = query.get("http://vehical_inform_consult.com/204/88/3.0.7)
        mycarNumber = sentence.re.findall("粤xxxxxx"，sentence)

        for item in mycarNumber
        	if mycarNumber[] == query[]
        		return {got_msg :1, {"您所查询的车辆号为："}}
        	else
        		return {got_msg :0, {"为获取到您的车辆号："}}
                return 

 		
        